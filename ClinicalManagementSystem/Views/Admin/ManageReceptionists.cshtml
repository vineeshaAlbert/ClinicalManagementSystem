@model IEnumerable<ClinicalManagementSystem.Models.Receptionist>
@{
    ViewBag.Title = "Manage Receptionists";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <h2 class="text-center mb-4">Manage Receptionists</h2>
    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger">@ViewBag.Error</div>
    }
    @if (ViewBag.Message != null)
    {
        <div class="alert alert-success">@ViewBag.Message</div>
    }

    <div class="d-flex justify-content-between mb-3">
        <a class="btn btn-secondary" href="@Url.Action("Index", "Admin")">← Back to Dashboard</a>
        <button class="btn btn-success" data-bs-toggle="modal" data-bs-target="#addReceptionistModal">+ Add Receptionist</button>
    </div>

    <table class="table table-bordered table-hover shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>Emp ID</th>
                <th>Name</th>
                <th>Contact</th>
                <th>Username</th>
                <th>Password</th>
                <th>Status</th> <!-- ✅ Toggle Column -->
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var r in Model)
            {
                <tr>
                    <td>@r.ReferenceId</td>
                    <td>@r.Name</td>
                    <td>@r.Phone</td>
                    <td>@r.Username</td>
                    <td>@r.Password</td>
                    <td>
                        <input type="checkbox" class="form-check-input toggle-active"
                               data-user-id="@r.ReceptionistId"
                        @(r.IsActive ? "checked" : "") />
                    </td>
                    <td>
                        <a href="#" class="btn btn-warning btn-sm" data-bs-toggle="modal" data-bs-target="#editReceptionistModal-@r.ReceptionistId">Edit</a>
                        <a href="@Url.Action("DeleteReceptionist", "Admin", new { id = r.ReceptionistId })" class="btn btn-danger btn-sm"
                           onclick="return confirm('Are you sure you want to delete this receptionist?');">
                            Delete
                        </a>
                    </td>
                </tr>

                <!-- Edit Modal -->
                <div class="modal fade" id="editReceptionistModal-@r.ReceptionistId" tabindex="-1" aria-labelledby="editReceptionistLabel" aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <form asp-action="EditReceptionist" method="post">
                                <input type="hidden" name="ReceptionistId" value="@r.ReceptionistId" />
                                <div class="modal-header bg-warning text-dark">
                                    <h5 class="modal-title">Edit Receptionist</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                </div>
                                <div class="modal-body">
                                    <div class="mb-3">
                                        <label class="form-label">Name</label>
                                        <input class="form-control" name="Name" value="@r.Name" required />
                                    </div>
                                    <div class="mb-3">
                                        <label class="form-label">Contact</label>
                                        <input class="form-control" name="Contact" value="@r.Phone" required />
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button class="btn btn-primary" type="submit">Save Changes</button>
                                    <button class="btn btn-secondary" type="button" data-bs-dismiss="modal">Cancel</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            }
        </tbody>
    </table>
</div>

<!-- Add Receptionist Modal -->
<div class="modal fade" id="addReceptionistModal" tabindex="-1" aria-labelledby="addReceptionistModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form asp-action="AddReceptionist" method="post">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title">Add Receptionist</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label">Name</label>
                        <input class="form-control" name="Name" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Contact</label>
                        <input class="form-control" name="Contact" required />
                    </div>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-success" type="submit">Add</button>
                    <button class="btn btn-secondary" type="button" data-bs-dismiss="modal">Cancel</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            // ✅ Toggle active status
            $('.toggle-active').change(function () {
                var userId = $(this).data('user-id');
                var isActive = $(this).is(':checked');
                $.post('/Admin/ToggleStatus', { userId: userId, isActive: isActive });
            });
        });
    </script>
}
